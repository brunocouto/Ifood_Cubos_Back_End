function soma(x, y) {
    //código
    const resultado = x + y;
    return resultado;
}


const soma = (x, y) => {
    //código
    const resultado = x + y;
    return resultado;
}
console.log(som(5, 2));