const saldo =10;

const unidade =( saldo === 1 ? "real ": "Reais");

console.log (`Voce tem saldo ${saldo} real `);

// if (saldo ===1 ){
//     unidade = "real"
// }else {
//     unidade ="reais";
// }