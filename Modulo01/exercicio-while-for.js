// criando um array de 5 numeros

const numeros = [0, 3, 18, 2, 11];

let i = 0;
// variavel que vai ser usada para percorrer o vetor
while (i < numeros.length) {
    console.log(numeros[i]);
    i++;
}